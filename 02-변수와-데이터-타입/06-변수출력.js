//출력할 변수 or 상수  정의

const a = "hello";
console.log(`${a} Javascript`);

// 백틱을 사용한 문자열 출력
const myName = "만수";
const myAge = 20;
const myMessage = `${myName}는 ${myAge} 세 입니다`;

console.log(myMessage);
